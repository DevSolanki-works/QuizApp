# Forge: AI Trivia Showdown — Backend Dependencies
# Pin versions for reproducible Docker builds

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1      # ASGI server; [standard] adds websocket support + speedups

# Pydantic (FastAPI uses this internally; we use it for AI output validation)
pydantic==2.10.3
pydantic-settings==2.6.1       # BaseSettings — reads from .env / env vars

# Google Gemini AI
google-generativeai==0.8.3

# HTTP client (used internally by google-generativeai)
httpx==0.28.1